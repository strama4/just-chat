import React, { Component } from 'react';

class Landing extends Component {
    render() {
        return (
            <section>
                Select a chat room from the side to see messages
            </section>
        );
    }
}

export default Landing;